name: Build RenderUtilitiz
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Gradle and source files
        run: |
          # Download gradle wrapper from Fabric template
          curl -L -o template.zip https://github.com/FabricMC/fabric-example-mod/archive/refs/heads/26.1.2.zip
          unzip -o template.zip
          cp -r fabric-example-mod-26.1.2/gradle .
          cp fabric-example-mod-26.1.2/gradlew .
          cp fabric-example-mod-26.1.2/gradlew.bat .
          cp fabric-example-mod-26.1.2/settings.gradle .
          cp fabric-example-mod-26.1.2/build.gradle .
          chmod +x gradlew
          rm -rf template.zip fabric-example-mod-26.1.2

          # Write gradle.properties with correct mod name
          cat > gradle.properties << 'EOF'
          org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G
          minecraft_version=26.1.2
          yarn_mappings=26.1.2+build.1
          loader_version=0.19.2
          fabric_version=0.102.0
          loom_version=1.16
          mod_version=26.1.2
          maven_group=com.example.renderutilitiz
          archives_base_name=renderutilitiz
          EOF

          # Create directory structure
          mkdir -p src/client/java/com/example/renderutilitiz
          mkdir -p src/client/resources/assets/renderutilitiz/lang

          # Write the main mod class with aim assist
          cat > src/client/java/com/example/renderutilitiz/RenderUtilitizMod.java << 'JAVA'
          package com.example.renderutilitiz;

          import net.fabricmc.api.ClientModInitializer;
          import net.fabricmc.api.EnvType;
          import net.fabricmc.api.Environment;
          import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
          import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
          import net.minecraft.client.KeyMapping;
          import net.minecraft.client.Minecraft;
          import net.minecraft.world.entity.Entity;
          import net.minecraft.world.entity.LivingEntity;
          import net.minecraft.world.phys.Vec3;
          import com.mojang.blaze3d.platform.InputConstants;
          import org.lwjgl.glfw.GLFW;
          import org.slf4j.Logger;
          import org.slf4j.LoggerFactory;

          @Environment(EnvType.CLIENT)
          public class RenderUtilitizMod implements ClientModInitializer {
              public static final String MOD_ID = "renderutilitiz";
              public static final Logger LOGGER = LoggerFactory.getLogger(MOD_ID);
              private static KeyMapping toggleKey;
              private static boolean isEnabled = true;
              private static long lastAimTime = 0;
              private static final long AIM_COOLDOWN_MS = 100;
              private static final double MAX_RANGE = 6.0;
              private static final double MAX_FOV = 45.0;
              private static final float AIM_SPEED = 0.35f;

              @Override
              public void onInitializeClient() {
                  toggleKey = KeyBindingHelper.registerKeyBinding(new KeyMapping(
                          "key.renderutilitiz.toggle",
                          InputConstants.Type.KEYSYM,
                          GLFW.GLFW_KEY_Y,
                          "category.renderutilitiz"
                  ));
                  ClientTickEvents.END_CLIENT_TICK.register(client -> {
                      if (toggleKey.consumeClick()) {
                          isEnabled = !isEnabled;
                          if (client.player != null) {
                              client.player.displayClientMessage(
                                  net.minecraft.network.chat.Component.literal(
                                      "RenderUtilitiz " + (isEnabled ? "§aON" : "§cOFF")
                                  ), true);
                          }
                      }
                      if (!isEnabled) return;
                      if (client.player == null || client.level == null) return;
                      if (!client.options.keyAttack.isDown()) return;
                      long now = System.currentTimeMillis();
                      if (now - lastAimTime < AIM_COOLDOWN_MS) return;
                      lastAimTime = now;
                      Entity target = findBestTarget(client);
                      if (target != null) smoothLookAt(client, target);
                  });
                  LOGGER.info("RenderUtilitiz initialized!");
              }

              private static Entity findBestTarget(Minecraft client) {
                  Entity best = null;
                  double bestScore = Double.MAX_VALUE;
                  for (Entity entity : client.level.entitiesForRendering()) {
                      if (entity == client.player) continue;
                      if (!(entity instanceof LivingEntity)) continue;
                      if (!entity.isAlive()) continue;
                      double dist = client.player.distanceTo(entity);
                      if (dist > MAX_RANGE) continue;
                      double fov = getAngleToEntity(client, entity);
                      if (fov > MAX_FOV) continue;
                      double score = dist * 0.7 + fov * 0.3;
                      if (score < bestScore) {
                          bestScore = score;
                          best = entity;
                      }
                  }
                  return best;
              }

              private static double getAngleToEntity(Minecraft client, Entity entity) {
                  Vec3 cameraVec = client.player.getViewVector(1.0f);
                  Vec3 toEntity = entity.getBoundingBox().getCenter()
                          .subtract(client.player.getEyePosition())
                          .normalize();
                  double dot = cameraVec.dot(toEntity);
                  dot = Math.max(-1.0, Math.min(1.0, dot));
                  return Math.toDegrees(Math.acos(dot));
              }

              private static void smoothLookAt(Minecraft client, Entity target) {
                  Vec3 targetPos = target.getBoundingBox().getCenter();
                  Vec3 playerEye = client.player.getEyePosition();
                  double dx = targetPos.x - playerEye.x;
                  double dy = targetPos.y - playerEye.y;
                  double dz = targetPos.z - playerEye.z;
                  double horizontal = Math.sqrt(dx*dx + dz*dz);
                  float targetYaw = (float) Math.toDegrees(Math.atan2(dz, dx)) - 90.0f;
                  float targetPitch = (float) -Math.toDegrees(Math.atan2(dy, horizontal));
                  float newYaw = client.player.getYRot() + (targetYaw - client.player.getYRot()) * AIM_SPEED;
                  float newPitch = client.player.getXRot() + (targetPitch - client.player.getXRot()) * AIM_SPEED;
                  client.player.setYRot(newYaw);
                  client.player.setXRot(newPitch);
              }
          }
          JAVA

          # Write fabric.mod.json
          cat > src/client/resources/fabric.mod.json << 'FABRIC'
          {
            "schemaVersion": 1,
            "id": "renderutilitiz",
            "version": "${version}",
            "name": "RenderUtilitiz",
            "description": "Aim assist mod",
            "authors": ["You"],
            "environment": "client",
            "entrypoints": {
              "client": ["com.example.renderutilitiz.RenderUtilitizMod"]
            },
            "depends": {
              "fabricloader": ">=0.16.0",
              "minecraft": "~26.1.2",
              "java": ">=25",
              "fabric-api": "*"
            }
          }
          FABRIC

          # Write language file
          cat > src/client/resources/assets/renderutilitiz/lang/en_us.json << 'LANG'
          {
            "key.renderutilitiz.toggle": "Toggle RenderUtilitiz",
            "category.renderutilitiz": "RenderUtilitiz"
          }
          LANG

      - name: Setup Java 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Build
        run: ./gradlew build --no-daemon

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: renderutilitiz-jar
          path: build/libs/*.jar
